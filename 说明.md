# 棕榈岛 · 手机网页版（单人）使用说明

## 运行方式

直接在浏览器打开 `index.html` 即可玩（纯前端，无需安装）。

推荐用本地服务器方式运行（手机和电脑都适用）：

```bash
python3 -m http.server 8080
```

然后手机/电脑浏览器访问 `http://localhost:8080`。

手机端建议：Chrome/Safari 打开后，用"添加到主屏幕"安装，即可离线游玩（PWA）。

## 已实现功能

- 完整单人基础规则：储存 / 旋转 / 翻面 / 弃牌、资源支付、4 张资源上限、资源过期、轮次结算、终局计分
- 天赋卡（Feat）系统：结算解锁 → 永久保存 → 新局携带 → 局内启用（得分加成 / 附加资源 / 免费升级）
- 中文界面：按钮、提示、规则速查、结算评级（10-19 有待改进 / 20-29 尚可 / 30-39 出色 / 40+ 惊人）
- 移动端竖屏布局：单指操作、卡片动画、防误触撤销、自动存档
- 新手引导（首次进入）、查看前两张背面、菜单（新游戏 / 帮助 / 记忆模式）
- PWA：可离线、可安装
- 规则引擎单元测试：`tests/engine.test.mjs`

## 天赋卡系统说明

- **解锁**：单局结束时自动检查天赋条件，达成即永久解锁（存档保存）
- **携带**：菜单 → 天赋图鉴 → 勾选本局携带的天赋；也可用难度预设快速选择
  - 简单：任意数量 / 中等：1 张 / 困难：0 张
- **使用**：携带的天赋会加入牌堆（轮次卡之前），轮到前两张时点"启用天赋"：
  - 得分型：直接生效，结算时加分
  - 资源型：获得附加资源（支付时自动优先使用，显示在资源区下方）
  - 免费升级型：选择前两张中的一张，免费旋转或翻面一次

## ⚠️ 卡牌数据待核对

本版本已实现全部规则与界面，但**每张卡的精确数值（各朝向的资源产出、升级费用、得分）为初稿**，
目前标记为"待核对"：

- 数据文件：`js/data/cards.js`，每张卡都有 `verified: false` 标记
- 卡牌种类、数量、升级几何关系（旋转 180° / 翻面）正确
- 具体费用/产出/分数需要以官方规则书卡面或实体卡为准

拿到准确数据后，只需修改 `js/data/cards.js` 一个文件即可，无需改动其他代码。

天赋卡同样为初稿（`js/data/feats.js`，`verified: false`）：
具体天赋名称、解锁条件与效果数值以官方天赋卡为准，只需修改该文件。

## 技术结构

```
index.html            页面
styles.css            样式（手机竖屏、扁平插画卡面）
manifest.webmanifest  PWA 配置
sw.js                 Service Worker（离线缓存）
icons/                应用图标
js/
  i18n.js             中文文案
  data/cards.js       卡牌数据（待核对）
  data/feats.js       天赋数据（待核对）
  engine.js           规则引擎（纯函数）
  ui.js               界面渲染与交互
  main.js             入口、存档、事件
tests/
  engine.test.mjs     引擎单元测试
```

运行测试：

```bash
node tests/engine.test.mjs
```
